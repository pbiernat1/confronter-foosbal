{% extends 'base.html.twig' %}

{% block body %}
<div class="container">
    <h1>Create new Player</h1>

    {{ include('player/_form.html.twig') }}

    <a class="btn btn-secondary" href="{{ path('player_index') }}">back to list</a>
</div>
{% endblock %}
